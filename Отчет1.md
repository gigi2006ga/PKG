Отчёт по лабораторной работе

Конвертер цветов: RGB ↔ CMYK ↔ HLS

🎯 Цели работы

Основные цели лабораторной работы:

Изучить теоретические основы цветовых моделей RGB, CMYK и HLS.

Реализовать точные алгоритмы преобразования между моделями.

Создать графическое приложение на Python (Tkinter) для работы с цветами.

Обеспечить автоматическую синхронизацию между всеми моделями при изменении любой из них.

Реализовать обработку ошибок и визуализацию результата.

📚 Теоретическое исследование цветовых моделей
🌈 RGB (Red, Green, Blue)

Аддитивная модель.

Используется на экранах и в цифровой графике.

Каждая компонента варьируется от 0 до 255.

Цвет формируется сложением трёх потоков света.

🖨 CMYK (Cyan, Magenta, Yellow, Key/Black)

Субтрактивная модель, используется в полиграфии.

Значения компонентов — от 0 до 1.

Цвет формируется поглощением света красками.

Формулы преобразования:

RGB → CMYK

C = 1 − R/255
M = 1 − G/255
Y = 1 − B/255
K = min(C, M, Y)
C = (C − K)/(1 − K)
M = (M − K)/(1 − K)
Y = (Y − K)/(1 − K)


CMYK → RGB

R = 255 × (1 − C) × (1 − K)
G = 255 × (1 − M) × (1 − K)
B = 255 × (1 − Y) × (1 − K)


Особенности:

Черный компонент K влияет сильно на итоговый цвет.

Преобразования сохраняют аддитивность и корректность диапазонов.

🎨 HLS (Hue, Lightness, Saturation)

Оттенок (H) — угол на цветовом круге (0–360°).

Светлота (L) — от 0 до 1.

Насыщенность (S) — от 0 до 1.

Удобно изменять отдельные характеристики цвета без изменения остальных.

Формулы преобразования RGB ↔ HLS включают нормализацию и расчёт через максимумы/минимумы компонент.

🔧 Реализация алгоритмов в проекте

В проекте включены функции:

rgb_to_cmyk(r, g, b) и cmyk_to_rgb(c, m, y, k)

rgb_to_hls(r, g, b) и hls_to_rgb(h, l, s)

Каждая функция:

Является чистой и независимой от интерфейса.

Выполняет строго математические преобразования.

Обеспечивает корректность значений (RGB: 0–255, CMYK: 0–1, HLS: H 0–360, L/S 0–1).

🖥 Интерфейс приложения

Приложение построено на Tkinter и содержит:

✳ Блок RGB

Ползунки для R, G, B (0–255)

Поля ввода для прямого изменения

События: изменение по Enter и при перетаскивании ползунка

✳ Блок CMYK

Ползунки для C, M, Y, K (0–1)

Поля ввода для чисел

Автоматический пересчёт RGB и HLS

✳ Блок HLS

Ползунки для H (0–360), L, S (0–1)

Поля ввода

Автоматический пересчёт RGB и CMYK

🎨 Визуализатор цвета

Отображает текущий выбранный цвет в прямоугольнике.

Обновляется при изменении любого значения.

🎛 Кнопка «Выбрать цвет»

Открывает стандартный colorpicker Tkinter

Обновляет все модели после выбора

🔁 Механизм автоматической синхронизации

Для предотвращения рекурсивного вызова обработчиков используется флаг self.updating = True/False.

При изменении любой модели:

Флаг активируется

Рассчитываются остальные модели

Обновляется интерфейс

Флаг сбрасывается

Особенности синхронизации:

Значения округляются с учётом диапазонов

Предотвращается дребезг при движении ползунков

RGB усечён при выходе за диапазон 0–255, появляется предупреждение

⚠ Обработка некорректных значений

Ввод ограничен допустимыми диапазонами

Любое значение автоматически приводится к диапазону

При недопустимых значениях RGB выводится предупреждение "RGB усечён!"

🧪 Тестирование
Тип теста	Пример	Результат
Чистые цвета	(255,0,0), (0,255,0), (0,0,255)	Корректно
Граничные значения	Белый, черный, серый	Без ошибок
Проверка CMYK ↔ RGB	(C=0.5, M=0.2, Y=0.3, K=0.1)	Полная корректность
Проверка HLS ↔ RGB	H=180, L=0.5, S=0.7	Точность до 0.1 единицы
Использование colorpicker	Любые цвета	Полная синхронизация всех моделей
Итоговый вывод

В ходе работы было разработано полноценное приложение-конвертер цветов, обеспечивающее:

Точные преобразования между RGB, CMYK и HLS

Автоматическую синхронизацию всех моделей

Отображение выбранного цвета в визуализаторе

Обработку ошибок и ограничение значений

Приложение полностью соответствует поставленным требованиям и может использоваться как практический инструмент для работы с цветовыми моделями в обучении и дизайне.
