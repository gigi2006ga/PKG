Отчёт по лабораторной работе

Чтение информации из графических файлов

🎯 Цели работы

Основные цели лабораторной работы:

Изучить методы получения информации о графических файлах (формат, размеры, глубина цвета, сжатие).

Реализовать алгоритмы обработки изображений с использованием JavaScript и веб-браузера.

Создать визуальный интерфейс в виде "папки" с карточками изображений для наглядного отображения данных.

Обеспечить динамическое отображение прогресса загрузки и обработки файлов.

Обработать ошибки при загрузке и чтении файлов.

📚 Теоретическое исследование
🖼 Формат и расширение файлов

Расширение (extension) — часть имени файла после точки, указывающая на его формат (например, jpg, png, bmp, gif).

Формат определяет способ хранения информации о пикселях, цветовой модели и сжатии изображения.

🌈 Глубина цвета (Color depth)

Показывает количество бит на один пиксель.

Примеры: 8 бит (GIF), 24 бита (JPEG/PNG с RGB), 32 бита (PNG с альфа-каналом).

Глубина цвета определяет количество возможных цветов: 2^бит.

📊 Сжатие изображений

JPEG — сжатие с потерями (lossy), уменьшается размер файла, но теряется часть информации.

PNG — сжатие без потерь (lossless, алгоритм LZ77).

GIF — сжатие без потерь (LZW), ограничено 256 цветами.

BMP — часто без сжатия.

🔧 Реализация алгоритмов в проекте

В проекте реализованы функции:

Загрузка файлов и папок:
Поддерживаются множественные файлы и структура папок через input с webkitdirectory.

Обработка изображений:
Для каждого файла получаем:

Имя файла

Формат (расширение)

Размер (ширина × высота)

Глубину цвета

Сжатие (для JPEG рассчитывается примерное значение в процентах, для других форматов — алгоритм или отсутствие сжатия)

Визуализация:
Файлы отображаются карточками с миниатюрой изображения и параметрами.

Прогресс загрузки:
Индикатор прогресса обновляется по мере обработки файлов.

Обработка ошибок:
Файлы, которые не удалось прочитать, отображаются с пометкой ошибки.

🖥 Интерфейс приложения

Приложение построено на HTML/CSS/JS и содержит:

✳ Левая панель (Sidebar)

Отображает текущую папку с иконкой 📁.

Может расширяться для отображения структуры папок (в перспективе).

✳ Правая панель (Main)

Кнопки загрузки файлов и папок

Прогресс-бар

Карточки изображений:

Миниатюра изображения

Имя файла

Расширение

Размер

Глубина цвета

Сжатие

🔁 Механизм динамического обновления

Обновление прогресса:
Используется progressFill.style.width для плавного изменения прогресса.

Добавление карточек:
Функции addCard(file, data) и addErrorCard(file) создают элементы DOM динамически.

Обработка метаданных:
С помощью библиотеки ExifReader читаются данные о изображении.

Поддержка форматов BMP, PNG, GIF, JPEG:
Для BMP и PNG вычисляется глубина цвета через чтение первых байтов файла.

JPEG: вычисляется примерное сжатие через соотношение размера файла к размеру несжатого изображения.

⚠ Обработка некорректных значений

Файлы без поддержки изображений игнорируются.

Ошибки при чтении файлов выводятся в консоль и отображаются карточкой с отметкой Ошибка при чтении.

Приложение устойчиво к отсутствию Exif-данных.

🧪 Тестирование
Тип теста	Пример	Результат
Файлы разных форматов	JPG, PNG, BMP, GIF	Все файлы обработаны корректно
Файлы без Exif	PNG, BMP	Не вызвало ошибок, глубина цвета определена вручную
Большая папка	50+ файлов	Прогресс-бар обновляется корректно, все файлы отображены
Неподдерживаемые файлы	TXT, PDF	Игнорируются без ошибки
Итоговый вывод

В ходе работы было разработано полноценное веб-приложение для анализа изображений.

Приложение корректно определяет имя файла, формат, размер, глубину цвета и тип сжатия.

Информация выводится в удобной форме карточек с миниатюрами.

Реализован прогресс-бар и обработка ошибок.

Визуальный интерфейс напоминает папку, что делает работу наглядной и удобной.

Приложение может служить инструментом для анализа изображений и учебной демонстрации метаданных.
